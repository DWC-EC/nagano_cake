<div class="mx-auto" style="max-width:900px;">
  <h4 class="d-flex justify-content-between text-light ml-5 mb-5">
    <span style="background-color:#adb5bd">　注文履歴詳細　</span>
  </h4>
  
<h2>注文情報</h2>
<div>
    <p>注文日：<%= @order.created_at.strftime('%Y/%m/%d') %></p>
    <p>配送先：〒<%= @order.postal_code %><br>
        <%= @order.address %><br>
        <%= @order.name %>
    </p>
    <p>支払方法：<%= @order.payment_method_i18n %></p>
</div>

<h2>請求情報</h2>
<div>
  <p>商品合計：<%= @order.billing_amount - @order.postage %>円</p>
  <p>配送料：<%= @order.postage %>円</p>
  <p>ご請求額：<%=  @order.billing_amount %>円</p>
</div>

<h2>注文内容</h2>
<div>
  <table>
    <thead>
      <tr>
        <th>商品</th>
        <th>単価(税込)</th>
        <th>個数</th>
        <th>小計</th>
      </tr>
    </thead>

    <tbody>
      <% @order_items.each do |order_item| %>
      <tr>
        <td><%= order_item.item.name %><!--order_itemとアソシエーションしたitemモデルからnemeを呼び出し--></td>
        <td><%= order_item.price %></td>
        <td><%= order_item.amount %></td>
        <td><%= order_item.price * order_item.amount %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
</div>