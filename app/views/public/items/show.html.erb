商品詳細
<div>
  <%= image_tag @item.get_image(200,200) %>
  <h1>商品名<%= @item.name %></h1>
  <p>商品説明<%= @item.introduction %></p>
  <p>¥<%= @item.add_tax_price.to_s(:dalimited) %>(税込)</p>
</div>
個数選択
カートに入れる