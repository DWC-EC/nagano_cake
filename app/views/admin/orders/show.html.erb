<h1>注文履歴詳細</h1>
<p>購入者：<%= @order.customer.last_name + @order.customer.first_name %></p>
<p>注文日：<%= @order.created_at.strftime('%Y/%m/%d %H:%M:%S') %></p>
<p>配送先：<%= @order.address %></p>
<p>支払方法：<%= @order.payment_method %></p>
<div>
  <table>
    <thead>
      <tr>
        <th>商品名</th>
        <th>単価(税込)</th>
        <th>数量</th>
        <th>小計</th>
      </tr>
    </thead>

    <tbody>
      <% @order_items.each do |order_item| %>
        <td><%= order_items.name %></td>
        <td><%= order_items.add_tax_price %></td>
        <td><%= order_items.amount %></td>
        <td><%#= order_items.subtotal %></td>
      <% end %>
    </tbody>
  </table>
</div>
<div>
  <p>商品合計：</p>
  <p>送料：</p>
  <p>請求金額合計：</p>
</div>