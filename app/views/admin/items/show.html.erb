<div class="mx-auto" style="max-width:900px;">

  <h4 class="d-flex text-light mb-3 ml-5">
    <span style="background-color:#adb5bd">　商品詳細　</span>
  </h4>

   <div class="d-flex justify-content-center mt-5 mb-5">
      <div class="mr-5">
        <%= image_tag @item.get_image(200,200) %>
      </div>
      <div>
        <div class="row mb-3">
          <div class="col-sm-3">商品名</div>
          <div class="col-sm-8"><%= @item.name %></div>
        </div>
        <div class="row mb-3">
          <div class="col-sm-3">商品説明</div>
          <div class="col-sm-8"><%= @item.introduction %></div>
        </div>
        <div class="row mb-3">
          <div class="col-sm-3">税込価格(税抜価格)</div>
          <div class="col-sm-8">
            <%= @item.add_tax_price.to_s(:delimited) %>
            (<%= @item.price.to_s(:delimited) %>)円
          </div>
        </div>
      </div>
  </div>

  <div class="text-center">
    <button class="btn btn-success">
      <%= link_to "編集する", edit_admin_item_path, class: "text-light px-3" %>
    </button>
  </div>

</div>